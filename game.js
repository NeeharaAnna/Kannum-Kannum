import { FaceLandmarker, FilesetResolver } from 'https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@1.0.1/vision_bundle.mjs';
const $ = id => document.getElementById(id); const stage=$('stage'), video=$('webcam'), target=$('target');
let landmarker, stream, raf, lastVideo=-1, mode='menu', style='human', baseline={x:.5,y:.5,hx:0,hy:0}, lastRaw=null, wrongSince=0, blinkSince=0, wasBlinking=false, violationReason='GAZE DIRECTION CHANGED', startedAt=0, level=1, blinks=0, lastDistraction=0, toleranceMs=1250, debugMode=false;
const messages=['LOOK LEFT.','HEY, WHAT\'S THAT?','YOUR PHONE IS RINGING.','Don\'t look at the timer.','Are you seriously still staring?','Blink if you\'re scared.','LOOK DOWN.','Something is behind you.'];
function status(text,color='var(--muted)') { $('status').firstChild.textContent=text; $('status').style.color=color; }
function best() { $('best-score').innerHTML=`${localStorage.getItem('kk-best-score')||0} <small>sec</small>`; $('best-level').textContent=localStorage.getItem('kk-best-level')||0; }
function rawGaze(result) { if(!result.faceLandmarks?.length) return null; const face=result.faceLandmarks[0]; const avg=(ids)=>ids.reduce((p,i)=>({x:p.x+face[i].x/ids.length,y:p.y+face[i].y/ids.length}),{x:0,y:0}); const leftIris=avg([468,469,470,471,472]),rightIris=avg([473,474,475,476,477]); const leftSpan=Math.max(.01,face[133].x-face[33].x),rightSpan=Math.max(.01,face[263].x-face[362].x); const leftCornerMid={x:(face[33].x+face[133].x)/2,y:(face[33].y+face[133].y)/2},rightCornerMid={x:(face[362].x+face[263].x)/2,y:(face[362].y+face[263].y)/2}; const eyeMid={x:(leftCornerMid.x+rightCornerMid.x)/2,y:(leftCornerMid.y+rightCornerMid.y)/2}; const eyeSpan=(leftSpan+rightSpan)/2; const leftX=(leftIris.x-face[33].x)/leftSpan,rightX=(rightIris.x-face[362].x)/rightSpan; const leftY=(leftIris.y-leftCornerMid.y)/leftSpan,rightY=(rightIris.y-rightCornerMid.y)/rightSpan; const openness=((face[145].y-face[159].y)/leftSpan+(face[374].y-face[386].y)/rightSpan)/2; return {eyesOpen:openness>.22,raw:{x:Math.max(0,Math.min(1,(leftX+rightX)/2)),y:(leftY+rightY)/2,hx:(face[1].x-eyeMid.x)/eyeSpan,hy:(face[1].y-eyeMid.y)/eyeSpan}}; }
function detect() { if(video.readyState>=2&&video.currentTime!==lastVideo) { lastVideo=video.currentTime; const sample=rawGaze(landmarker.detectForVideo(video,performance.now())); if(sample?.eyesOpen===false) { if(!blinkSince) { blinkSince=Date.now(); if(!wasBlinking) blinks++; } wasBlinking=true; if(mode==='playing') gameOver('BLINKING DETECTED'); } else if(sample) { blinkSince=0; wasBlinking=false; lastRaw=lastRaw?{x:lastRaw.x*.72+sample.raw.x*.28,y:lastRaw.y*.72+sample.raw.y*.28,hx:lastRaw.hx*.72+sample.raw.hx*.28,hy:lastRaw.hy*.72+sample.raw.hy*.28}:sample.raw; const headX=Math.abs(lastRaw.hx-baseline.hx)>.08?(lastRaw.hx-baseline.hx)*.4:0; const headY=Math.abs(lastRaw.hy-baseline.hy)>.08?(lastRaw.hy-baseline.hy)*.3:0; const pupilShift=Math.hypot((lastRaw.x-baseline.x)*3.2,(lastRaw.y-baseline.y)*5), headShift=Math.hypot(headX,headY); if(headShift>pupilShift&&headShift>.05) violationReason='HEAD MOVEMENT DETECTED'; else violationReason='GAZE DIRECTION CHANGED'; const verticalLook=(lastRaw.y-baseline.y)*6.5+headY; const screen={x:Math.max(0,Math.min(1,.5+(lastRaw.x-baseline.x)*3.2+headX)),y:Math.max(0,Math.min(1,.5+verticalLook))}; $('gaze-dot').style.left=`${screen.x*100}%`; $('gaze-dot').style.top=`${screen.y*100}%`; if(mode==='playing'&&(lastRaw.y<baseline.y-.008||lastRaw.hy<baseline.hy-.03)) gameOver('GAZE ABOVE THE EYE'); else if(mode==='playing') checkGaze(screen); if(debugMode) $('debug').innerHTML=`GAZE: ${screen.x<.4?'LEFT':screen.x>.6?'RIGHT':screen.y<.4?'UP':screen.y>.6?'DOWN':'CENTER'}<br>IRIS X: ${lastRaw.x.toFixed(3)}<br>IRIS Y: ${lastRaw.y.toFixed(3)}<br>HEAD X: ${lastRaw.hx.toFixed(3)}<br>HEAD Y: ${lastRaw.hy.toFixed(3)}<br>EYES: OPEN<br>ZONE: ${mode==='playing'&&!wrongSince?'SAFE':'WATCHING'}<br>LOOK-AWAY: ${wrongSince?((Date.now()-wrongSince)/1000).toFixed(1)+'s':'0.0s'}`; } else if(mode==='playing') { if(!blinkSince) blinkSince=Date.now(); if(Date.now()-blinkSince>1800) gameOver('FACE TRACKING LOST'); } } raf=requestAnimationFrame(detect); }
function checkGaze(gaze) { const distance=Math.hypot(gaze.x-.5,gaze.y-.5), limit=Math.max(.075,.12-level*.004), ok=distance<limit; const elapsed=(Date.now()-startedAt)/1000; $('timer').textContent=`${String(Math.floor(elapsed/60)).padStart(2,'0')}:${(elapsed%60).toFixed(2).padStart(5,'0')}`; $('score').textContent=String(Math.floor(elapsed*10+level*5)).padStart(3,'0'); $('meter-fill').style.transform=`scaleX(${Math.max(0,Math.min(1,1-distance/limit))})`; $('meter-fill').style.background=ok?'var(--mint-deep)':'var(--coral)'; if(ok) { wrongSince=0; status(level>3?'THE EYE IS IMPRESSED.':'HOLD YOUR GAZE'); } else { wrongSince=Date.now(); status('GAZE MOVED','var(--coral)'); gameOver(violationReason); } if(elapsed>level*10) { level=Math.min(9,Math.floor(elapsed/10)+1); $('round-label').textContent=`LEVEL ${level}`; } }
function distract() { if(mode!=='playing'||Date.now()-lastDistraction<3200) return; const elapsed=(Date.now()-startedAt)/1000; if(elapsed<8||Math.random()>.34+level*.04) return; lastDistraction=Date.now(); const d=document.createElement('div'); d.className=`distraction ${Math.random()>.7?'alert':''}`; d.textContent=messages[Math.floor(Math.random()*messages.length)]; d.style.left=`${8+Math.random()*70}%`; d.style.top=`${13+Math.random()*68}%`; if(Math.abs(parseFloat(d.style.left)-50)<18) d.style.left='8%'; stage.appendChild(d); if(level>4&&Math.random()>.5) stage.classList.add('screen-flash'); setTimeout(()=>d.remove(),Math.min(4200,1800+level*230)); setTimeout(()=>stage.classList.remove('screen-flash'),450); }
function audio(pitch=320) { try { const c=new AudioContext(),o=c.createOscillator(),g=c.createGain(); o.frequency.value=pitch; g.gain.setValueAtTime(.04,c.currentTime); g.gain.exponentialRampToValueAtTime(.001,c.currentTime+.18); o.connect(g).connect(c.destination); o.start(); o.stop(c.currentTime+.18); } catch {} }
function gameOver(reason) { if(mode!=='playing') return; mode='over'; target.classList.add('annoyed'); $('timer').classList.add('hidden'); $('zone').classList.add('hidden'); $('gaze-dot').classList.add('hidden'); const seconds=(Date.now()-startedAt)/1000; const old=+(localStorage.getItem('kk-best-score')||0); const oldLevel=+(localStorage.getItem('kk-best-level')||0); localStorage.setItem('kk-best-score',Math.max(old,Math.floor(seconds))); localStorage.setItem('kk-best-level',Math.max(oldLevel,level)); best(); audio(130); $('welcome').classList.remove('hidden'); $('round-label').textContent='RESULTS'; $('welcome').innerHTML=`<div><div class="eyebrow">${reason}</div><h2>GAZE LOST.</h2><p>I saw that. You survived <b>${seconds.toFixed(2)} seconds</b>, reached level ${level}, and blinked ${blinks} time${blinks===1?'':'s'}. The eye wins this round.</p><button class="primary" id="retry">RETRY <span aria-hidden="true">↗</span></button><button class="ghost" id="menu">MAIN MENU</button></div>`; $('retry').onclick=()=>{menu();begin();}; $('menu').onclick=menu; }
async function calibrate() { mode='calibrating'; $('round-label').textContent='CALIBRATION'; $('welcome').classList.add('hidden'); const dot=$('cal-dot'); dot.classList.remove('hidden'); dot.style.left='50%'; dot.style.top='50%'; status('LOOK DIRECTLY AT THE CENTER'); let sum={x:0,y:0,hx:0,hy:0},samples=0; await new Promise(r=>setTimeout(r,700)); const end=Date.now()+2200; while(Date.now()<end) { if(lastRaw){sum.x+=lastRaw.x;sum.y+=lastRaw.y;sum.hx+=lastRaw.hx;sum.hy+=lastRaw.hy;samples++;} await new Promise(r=>setTimeout(r,100)); } dot.classList.add('hidden'); baseline={x:samples?sum.x/samples:.5,y:samples?sum.y/samples:.5,hx:samples?sum.hx/samples:0,hy:samples?sum.hy/samples:0}; mode='playing'; startedAt=Date.now(); level=1; blinks=0; wasBlinking=false; wrongSince=0; $('timer').classList.remove('hidden'); $('zone').classList.remove('hidden'); $('gaze-dot').classList.remove('hidden'); $('round-label').textContent='LEVEL 1'; status('HOLD YOUR GAZE'); audio(520); }
async function begin() { $('start').disabled=true; $('start').textContent='OPENING CAMERA...'; try { if(!stream) { stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:'user',width:640,height:480},audio:false}); video.srcObject=stream; await video.play(); } $('connection').textContent='CAMERA ONLINE'; status('LOADING FACE MODEL'); if(!landmarker) landmarker=await FaceLandmarker.createFromOptions(await FilesetResolver.forVisionTasks('https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@1.0.1/wasm'),{baseOptions:{modelAssetPath:'https://storage.googleapis.com/mediapipe-models/face_landmarker/face_landmarker/float16/1/face_landmarker.task',delegate:'GPU'},runningMode:'VIDEO',numFaces:1,minFaceDetectionConfidence:.5,minTrackingConfidence:.5}); if(!raf) detect(); await calibrate(); } catch(e) { console.error(e); $('connection').textContent='CAMERA NEEDED'; status('CAMERA BLOCKED','var(--coral)'); $('welcome').classList.remove('hidden'); $('welcome').querySelector('p').textContent='Face tracking needs camera permission. Allow access, sit in front of the camera, then try again.'; $('start').disabled=false; $('start').textContent='TRY CAMERA AGAIN →'; } }
function menu() { mode='menu'; $('welcome').classList.remove('hidden'); $('round-label').textContent='MAIN MENU'; $('timer').classList.add('hidden'); $('zone').classList.add('hidden'); $('gaze-dot').classList.add('hidden'); $('target').classList.remove('annoyed'); $('welcome').innerHTML='<div><div class="eyebrow">കണ്ണും കണ്ണും</div><h2>How long can you keep looking?</h2><p>Camera processing stays in this browser. No video is recorded or uploaded.</p><button class="primary" id="start">START <span aria-hidden="true">→</span></button><button class="ghost" id="style-button">EYE STYLE</button><button class="ghost" id="settings-button">SETTINGS</button></div>'; bindMenu(); }
function bindMenu() { $('start').onclick=begin; $('style-button').onclick=()=> $('style-modal').classList.remove('hidden'); $('settings-button').onclick=()=> $('settings-modal').classList.remove('hidden'); }
document.querySelectorAll('.choice').forEach(b=>b.onclick=()=>{style=b.dataset.eye; target.dataset.style=style; document.querySelectorAll('.choice').forEach(x=>x.classList.toggle('active',x===b));}); $('close-style').onclick=()=> $('style-modal').classList.add('hidden'); $('close-settings').onclick=()=>{toleranceMs=+$('tolerance-input').value;debugMode=$('debug-input').checked;$('debug').classList.toggle('hidden',!debugMode);$('settings-modal').classList.add('hidden');}; bindMenu(); best(); setInterval(distract,900);
